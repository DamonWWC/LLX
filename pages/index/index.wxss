/**index.wxss - 淘宝风格**/
page {
  background-color: #f5f5f5;
  height: 100vh;
  width: 100%;
  overflow-x: hidden;
}

.container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  width: 100%;
  max-width: 100vw;
  overflow-x: hidden;
  box-sizing: border-box;
}

/* 顶部导航栏 - 淘宝风格（固定顶部） */
.top-bar {
  background: linear-gradient(135deg, #ff6034 0%, #ee0a24 100%);
  padding: 30rpx 24rpx 25rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
  box-shadow: 0 2rpx 12rpx rgba(238, 10, 36, 0.2);
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  width: 100%;
  max-width: 100vw;
  box-sizing: border-box;
}

.top-left {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.page-title {
  font-size: 38rpx;
  font-weight: bold;
  color: #ffffff;
  letter-spacing: 2rpx;
}

.restore-btn {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.9);
  background-color: rgba(255, 255, 255, 0.15);
  padding: 8rpx 16rpx;
  border-radius: 20rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.3);
  transition: all 0.2s;
}

.restore-btn:active {
  background-color: rgba(255, 255, 255, 0.25);
  transform: scale(0.95);
}

.add-product-btn {
  background-color: rgba(255, 255, 255, 0.95);
  border-radius: 36rpx;
  padding: 14rpx 28rpx;
  display: flex;
  align-items: center;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  transition: all 0.3s;
}

.add-product-btn:active {
  transform: scale(0.95);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.1);
}

.add-icon {
  font-size: 30rpx;
  color: #ff6034;
  font-weight: bold;
  margin-right: 8rpx;
  line-height: 1;
}

.add-text {
  font-size: 26rpx;
  color: #ff6034;
  font-weight: 600;
}

/* 商品列表 - 淘宝卡片风格 */
.product-list {
  flex: 1;
  overflow-y: scroll;
  overflow-x: hidden;
  padding: 130rpx 0 180rpx;
  background-color: #f5f5f5;
  width: 100%;
  max-width: 100vw;
  box-sizing: border-box;
}

.product-item {
  background-color: #ffffff;
  border-radius: 0;
  padding: 24rpx 24rpx;
  margin-bottom: 2rpx;
  display: flex;
  align-items: center;
  box-shadow: none;
  position: relative;
  transition: all 0.2s;
  border: none;
  width: 100%;
  max-width: 100vw;
  box-sizing: border-box;
}

.product-item:active {
  background-color: #fafafa;
}

/* 商品图片 - 淘宝风格 */
.product-image {
  width: 180rpx;
  height: 180rpx;
  border-radius: 12rpx;
  background-color: #fafafa;
  margin-right: 20rpx;
  flex-shrink: 0;
  border: none;
}

/* 商品信息 */
.product-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  min-width: 0;
}

.product-name {
  font-size: 30rpx;
  font-weight: 500;
  color: #333333;
  margin-bottom: 10rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  line-height: 1.4;
}

.product-price {
  display: flex;
  align-items: baseline;
  margin-bottom: 8rpx;
}

.price-value {
  font-size: 48rpx;
  font-weight: bold;
  color: #ff6034;
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", Helvetica,
    Arial, sans-serif;
  line-height: 1;
}

.price-value::before {
  content: "¥";
  font-size: 30rpx;
  margin-right: 2rpx;
}

.price-unit {
  font-size: 22rpx;
  color: #999999;
  margin-left: 4rpx;
}

.product-weight {
  font-size: 22rpx;
  color: #666666;
  background-color: #f7f8fa;
  padding: 6rpx 12rpx;
  border-radius: 6rpx;
  display: inline-block;
  width: fit-content;
}

/* 数量控制 - 电商风格 */
.quantity-control {
  display: flex;
  align-items: center;
  margin-left: 24rpx;
  background-color: #f7f8fa;
  border-radius: 40rpx;
  padding: 6rpx;
}

.control-btn {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background-color: #ffffff;
  transition: all 0.2s;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.06);
}

.control-btn:active {
  transform: scale(0.9);
}

.control-btn.minus {
  background-color: #ffffff;
  border: 1rpx solid #e5e5e5;
}

.control-btn.minus.disabled {
  background-color: #fafafa;
  opacity: 0.5;
  border-color: #f0f0f0;
}

.control-btn.plus {
  background: linear-gradient(135deg, #ff6034 0%, #ee0a24 100%);
  box-shadow: 0 4rpx 12rpx rgba(255, 96, 52, 0.3);
}

.control-icon {
  font-size: 32rpx;
  font-weight: bold;
  color: #999999;
  line-height: 1;
}

.control-btn.plus .control-icon {
  color: #ffffff;
}

.quantity-display {
  min-width: 56rpx;
  text-align: center;
  font-size: 30rpx;
  font-weight: bold;
  color: #333333;
  margin: 0 12rpx;
}

/* 删除按钮 - 优化位置 */
.delete-icon {
  position: absolute;
  top: 20rpx;
  right: 20rpx;
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  background-color: rgba(0, 0, 0, 0.05);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  color: #999999;
  transition: all 0.2s;
}

.delete-icon:active {
  background-color: #ff4d4f;
  color: #ffffff;
  transform: rotate(90deg);
}

/* 空状态 */
.empty-state {
  text-align: center;
  padding: 150rpx 0;
  margin-top: 130rpx;
}

.empty-icon {
  font-size: 120rpx;
  display: block;
  margin-bottom: 30rpx;
  opacity: 0.5;
}

.empty-text {
  font-size: 32rpx;
  color: #999999;
  display: block;
  margin-bottom: 15rpx;
}

.empty-hint {
  font-size: 26rpx;
  color: #cccccc;
  display: block;
}

/* 底部购物车栏 - 淘宝结算栏风格 */
.cart-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background-color: #ffffff;
  padding: 16rpx 24rpx;
  padding-bottom: calc(16rpx + env(safe-area-inset-bottom));
  display: flex;
  justify-content: space-between;
  align-items: center;
  box-shadow: 0 -4rpx 20rpx rgba(0, 0, 0, 0.08);
  z-index: 999;
  border-top: 1rpx solid #e5e5e5;
  width: 100%;
  max-width: 100vw;
  box-sizing: border-box;
}

.cart-info {
  display: flex;
  align-items: center;
  flex: 1;
}

.cart-icon-wrapper {
  position: relative;
  margin-right: 24rpx;
}

.cart-icon {
  font-size: 56rpx;
}

.cart-badge {
  position: absolute;
  top: -6rpx;
  right: -10rpx;
  background: linear-gradient(135deg, #ff6034 0%, #ee0a24 100%);
  color: #ffffff;
  font-size: 20rpx;
  font-weight: bold;
  padding: 4rpx 10rpx;
  border-radius: 20rpx;
  min-width: 36rpx;
  text-align: center;
  box-shadow: 0 2rpx 8rpx rgba(255, 96, 52, 0.4);
  border: 2rpx solid #ffffff;
}

.cart-text {
  display: flex;
  flex-direction: column;
}

.cart-label {
  font-size: 22rpx;
  color: #999999;
  margin-bottom: 4rpx;
}

.cart-count {
  font-size: 30rpx;
  font-weight: bold;
  color: #333333;
}

.cart-count::after {
  content: "件商品";
  font-size: 24rpx;
  font-weight: normal;
  color: #666666;
  margin-left: 4rpx;
}

.cart-actions {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.clear-cart-btn {
  padding: 18rpx 28rpx;
  border: 2rpx solid #ff6034;
  border-radius: 40rpx;
  color: #ff6034;
  font-size: 26rpx;
  font-weight: 500;
  transition: all 0.2s;
}

.clear-cart-btn:active {
  background-color: #fff5f5;
  transform: scale(0.95);
}

.checkout-btn {
  background-color: #d0d0d0;
  color: #ffffff;
  padding: 22rpx 52rpx;
  border-radius: 50rpx;
  font-size: 32rpx;
  font-weight: bold;
  transition: all 0.3s;
  box-shadow: none;
  letter-spacing: 2rpx;
}

.checkout-btn.active {
  background: linear-gradient(135deg, #ff6034 0%, #ee0a24 100%);
  box-shadow: 0 4rpx 20rpx rgba(255, 96, 52, 0.5);
  transform: scale(1);
}

.checkout-btn.active:active {
  transform: scale(0.96);
  box-shadow: 0 2rpx 12rpx rgba(255, 96, 52, 0.5);
}

/* 计算结果区域 - 淘宝订单风格 */
.result-section {
  position: fixed;
  bottom: 140rpx;
  left: 0;
  right: 0;
  background-color: #ffffff;
  border-radius: 24rpx 24rpx 0 0;
  box-shadow: 0 -4rpx 40rpx rgba(0, 0, 0, 0.15);
  max-height: 65vh;
  overflow: hidden;
  z-index: 998;
  animation: slideUp 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  width: 100%;
  max-width: 100vw;
  box-sizing: border-box;
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(80rpx) scale(0.95);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.result-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx 32rpx 24rpx;
  border-bottom: 1rpx solid #f0f0f0;
  background: linear-gradient(180deg, #fafafa 0%, #ffffff 100%);
}

.result-title {
  font-size: 34rpx;
  font-weight: bold;
  color: #333333;
}

.result-title::before {
  content: "📋";
  margin-right: 12rpx;
}

.close-result {
  font-size: 44rpx;
  color: #cccccc;
  line-height: 1;
  padding: 8rpx;
  transition: all 0.2s;
}

.close-result:active {
  color: #999999;
  transform: rotate(90deg);
}

.result-content {
  padding: 24rpx 32rpx 32rpx;
  max-height: 55vh;
  overflow-y: scroll;
}

/* 收货地址区域 */
.address-section {
  background-color: #fff5f0;
  border-radius: 16rpx;
  padding: 24rpx;
  margin-bottom: 24rpx;
  border: 2rpx solid #ff6034;
  transition: all 0.2s;
}

.address-section:active {
  background-color: #ffe8e0;
  transform: scale(0.98);
}

.address-header-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
}

.address-title {
  font-size: 28rpx;
  font-weight: bold;
  color: #ff6034;
}

.address-action {
  font-size: 26rpx;
  color: #ff6034;
}

.address-content {
  padding-top: 12rpx;
}

.address-info-row {
  display: flex;
  align-items: center;
  margin-bottom: 12rpx;
}

.address-name {
  font-size: 30rpx;
  font-weight: bold;
  color: #333333;
  margin-right: 20rpx;
}

.address-phone {
  font-size: 26rpx;
  color: #666666;
}

.address-detail-text {
  font-size: 26rpx;
  color: #666666;
  line-height: 1.6;
}

.address-empty {
  text-align: center;
  padding: 20rpx 0;
}

.empty-hint-text {
  font-size: 26rpx;
  color: #999999;
}

.result-items {
  margin-bottom: 24rpx;
}

.items-title {
  font-size: 28rpx;
  font-weight: bold;
  color: #333333;
  margin-bottom: 16rpx;
}

.result-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20rpx;
  background-color: #fafafa;
  border-radius: 12rpx;
  margin-bottom: 12rpx;
}

.item-name {
  font-size: 28rpx;
  color: #666666;
  flex: 1;
}

.item-price {
  font-size: 30rpx;
  color: #333333;
  font-weight: 600;
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", Helvetica,
    Arial, sans-serif;
}

.result-divider {
  height: 1rpx;
  background-color: #e5e5e5;
  margin: 24rpx 0;
}

.result-summary {
  margin-bottom: 24rpx;
}

.summary-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 0;
  font-size: 28rpx;
}

.summary-label {
  color: #666666;
}

.summary-value {
  color: #333333;
  font-weight: 500;
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", Helvetica,
    Arial, sans-serif;
}

.summary-row.total {
  padding: 24rpx 0 16rpx;
  border-top: 2rpx solid #ff6034;
  margin-top: 16rpx;
  background: linear-gradient(90deg, #fff5f5 0%, #ffffff 100%);
  padding-left: 20rpx;
  padding-right: 20rpx;
  margin-left: -20rpx;
  margin-right: -20rpx;
  border-radius: 12rpx;
}

.summary-row.total .summary-label {
  font-size: 32rpx;
  font-weight: bold;
  color: #333333;
}

.summary-row.total .summary-label::before {
  content: "实付款";
  font-size: 24rpx;
  color: #999999;
  margin-right: 8rpx;
}

.summary-row.total .summary-value.grand {
  font-size: 44rpx;
  font-weight: bold;
  color: #ff6034;
}

.summary-row.total .summary-value.grand::before {
  content: "¥";
  font-size: 32rpx;
  margin-right: 4rpx;
}

.result-actions {
  margin-top: 24rpx;
  display: flex;
  gap: 16rpx;
}

.save-image-btn {
  flex: 1;
  background: linear-gradient(135deg, #07c160 0%, #06ae56 100%);
  color: #ffffff;
  border: none;
  border-radius: 48rpx;
  padding: 24rpx;
  font-size: 28rpx;
  font-weight: bold;
  box-shadow: 0 8rpx 24rpx rgba(7, 193, 96, 0.3);
  transition: all 0.2s;
}

.save-image-btn.secondary {
  background: #f7f8fa;
  color: #666666;
  box-shadow: none;
  border: 2rpx solid #e5e5e5;
}

.save-image-btn.secondary:active {
  background-color: #e8e8e8;
}

.save-image-btn::before {
  content: "📷 ";
  margin-right: 8rpx;
}

.save-image-btn:active {
  transform: scale(0.98);
  box-shadow: 0 4rpx 16rpx rgba(7, 193, 96, 0.3);
}

.confirm-order-btn {
  flex: 2;
  background: linear-gradient(135deg, #ff6034 0%, #ee0a24 100%);
  color: #ffffff;
  border: none;
  border-radius: 48rpx;
  padding: 24rpx;
  font-size: 32rpx;
  font-weight: bold;
  box-shadow: 0 8rpx 24rpx rgba(255, 96, 52, 0.4);
  transition: all 0.2s;
}

.confirm-order-btn:active {
  transform: scale(0.98);
  box-shadow: 0 4rpx 16rpx rgba(255, 96, 52, 0.4);
}

/* 弹窗遮罩 - 优化动画 */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.6);
  z-index: 1000;
  animation: fadeIn 0.3s;
  backdrop-filter: blur(4rpx);
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

/* 弹窗主体 - 更精致 */
.modal-dialog {
  position: fixed;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  width: 660rpx;
  max-height: 85vh;
  background-color: #ffffff;
  border-radius: 24rpx;
  z-index: 1001;
  animation: slideIn 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  box-shadow: 0 24rpx 48rpx rgba(0, 0, 0, 0.2);
}

@keyframes slideIn {
  from {
    opacity: 0;
    transform: translate(-50%, -40%) scale(0.9);
  }
  to {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1);
  }
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 40rpx 40rpx 30rpx;
  border-bottom: 1rpx solid #f0f0f0;
  background: linear-gradient(180deg, #fafafa 0%, #ffffff 100%);
}

.modal-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #333333;
}

.modal-title::before {
  content: "✨";
  margin-right: 12rpx;
}

.modal-close {
  font-size: 44rpx;
  color: #cccccc;
  line-height: 1;
  padding: 8rpx;
  transition: all 0.2s;
}

.modal-close:active {
  color: #999999;
  transform: rotate(90deg);
}

.modal-content {
  padding: 40rpx;
  max-height: 60vh;
  overflow-y: scroll;
}

.form-item {
  margin-bottom: 32rpx;
}

.form-label {
  font-size: 28rpx;
  color: #666666;
  margin-bottom: 16rpx;
  display: flex;
  align-items: center;
  font-weight: 500;
}

.label-hint {
  font-size: 22rpx;
  color: #999999;
  font-weight: normal;
  margin-left: 8rpx;
}

.required {
  color: #ff6034;
  margin-right: 8rpx;
  font-size: 32rpx;
}

.form-input {
  background-color: #f7f8fa;
  border: 2rpx solid #e5e5e5;
  border-radius: 16rpx;
  padding: 24rpx 28rpx;
  font-size: 28rpx;
  width: 100%;
  box-sizing: border-box;
  transition: all 0.3s;
}

/* 单选框组 */
.radio-group {
  display: flex;
  gap: 32rpx;
  padding: 12rpx 0;
}

.radio-item {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.radio-text {
  font-size: 28rpx;
  color: #333333;
}

.form-input:focus {
  border-color: #ff6034;
  background-color: #ffffff;
  box-shadow: 0 0 0 4rpx rgba(255, 96, 52, 0.1);
}

.image-picker {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #f7f8fa 0%, #ffffff 100%);
  border: 2rpx dashed #d0d0d0;
  border-radius: 16rpx;
  padding: 32rpx;
  transition: all 0.3s;
}

.image-picker:active {
  background-color: #f0f0f0;
  border-color: #ff6034;
}

.preview-image {
  width: 200rpx;
  height: 200rpx;
  border-radius: 16rpx;
  background-color: #e0e0e0;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.08);
}

.picker-hint {
  font-size: 26rpx;
  color: #666666;
  font-weight: 500;
}

.picker-hint::before {
  content: "📷 ";
  margin-right: 4rpx;
}

.picker-sub-hint {
  font-size: 22rpx;
  color: #999999;
  margin-top: 8rpx;
}

.modal-footer {
  display: flex;
  padding: 30rpx 40rpx 40rpx;
  gap: 20rpx;
  background-color: #fafafa;
  border-radius: 0 0 24rpx 24rpx;
}

.modal-btn {
  flex: 1;
  border: none;
  border-radius: 48rpx;
  padding: 28rpx;
  font-size: 32rpx;
  font-weight: bold;
  transition: all 0.2s;
}

.modal-btn.cancel {
  background-color: #f0f0f0;
  color: #666666;
}

.modal-btn.cancel:active {
  background-color: #e0e0e0;
  transform: scale(0.98);
}

.modal-btn.confirm {
  background: linear-gradient(135deg, #ff6034 0%, #ee0a24 100%);
  color: #ffffff;
  box-shadow: 0 8rpx 24rpx rgba(255, 96, 52, 0.3);
}

.modal-btn.confirm:active {
  transform: scale(0.98);
  box-shadow: 0 4rpx 16rpx rgba(255, 96, 52, 0.3);
}

/* Canvas（隐藏） */
.result-canvas {
  position: fixed;
  left: -9999px;
  top: -9999px;
  opacity: 0;
}
